import React from 'react';

// This app contains 3 types of users: user, organizer, and administrator
// Organizers are registered as regular users first then can request to the adminsitrator to be an organizer
// Administrator manages the app granting regular users to be an organizer
const Register = () => {
  return <div>Register</div>;
};

export default Register;
